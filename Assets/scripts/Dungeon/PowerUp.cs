using UnityEngine;

public class PowerUp : MonoBehaviour  // Để có Thể Thừa Các Vật Phẩm Rơi Ra
{
    public Signal ItemSingal;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
